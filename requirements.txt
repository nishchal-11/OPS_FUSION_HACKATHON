# Core Data Science & ML (Streamlit Cloud friendly)
numpy>=1.24.4,<2.3
pandas>=2.0
scikit-learn>=1.2
matplotlib>=3.7
seaborn>=0.12

# Audio Processing
librosa>=0.10.0
soundfile>=0.12
audioread>=3.0.0

# Machine Learning (CPU build for Streamlit Cloud)
tensorflow-cpu==2.15.0
protobuf>=3.20.3,<5.0.0
tensorboard>=2.15

# Additional ML utilities
joblib>=1.2

# Web App & UI
streamlit>=1.29

# QR Code Generation
qrcode[pil]>=7.4
Pillow>=10.0

# Jupyter & Development (optional; safe on Cloud but not required)
jupyter>=1.0.0
ipykernel>=6.0.0
notebook>=7.0.0

# Utilities
tqdm>=4.66.0
python-dotenv>=1.0.0
requests>=2.31.0

# Notes:
# - Removed tensorflow-io and streamlit-webrtc to avoid build issues and because they are not used.
# - Using tensorflow-cpu for faster, more reliable Streamlit Cloud deployments.